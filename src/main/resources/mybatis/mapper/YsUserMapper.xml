<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ggman.photo.mapper.YsUserMapper">
    <select id="getAll" resultType="com.ggman.photo.bean.YsUser">
        SELECT  * FROM  ys_user  WHERE  id=#{id} and sign=0
    </select>


</mapper>